<header class="navbar sticky">
  <div class="nav-container">
    <a href="/" class="logo">Math<span>Club</span></a>

    <nav class="nav-links">
      <a href="/">Bosh sahifa</a>
      <a href="/about">Biz haqimizda</a>
      <a href="/register-course">Kursga yozilish</a>
      <a href="/videos">Video darsliklar</a>
      <% if (user) { %>
        <a href="/tests">Testlar</a>
        <a href="/leaderboard">Yetakchilar</a>
        <a href="/messages" class="nav-link-with-badge">
          Adminga xabarlashish
          <% if (typeof unreadFromAdmin === 'number' && unreadFromAdmin > 0) { %>
            <span class="nav-badge"><%= unreadFromAdmin %></span>
          <% } %>
        </a>
      <% } %>
    </nav>

    <div class="nav-right">
      <% if (!user) { %>
        <a href="/auth/login" class="btn-outline">Kirish</a>
        <a href="/auth/register" class="btn-primary">Ro‘yxatdan o‘tish</a>
      <% } else { %>
        <!-- Desktop uchun profil + stars -->
        <div class="nav-profile">
          <span class="nav-stars">⭐ <%= typeof user.starsBalance === 'number' ? user.starsBalance : 0 %></span>
          <span class="nav-username"><%= user.name || 'Foydalanuvchi' %></span>
          <div class="avatar-dropdown">
            <div class="avatar-circle">
              <% if (user.avatar) { %>
                <img src="<%= user.avatar %>" alt="avatar" />
              <% } else { %>
                <span><%= user.name ? user.name.charAt(0).toUpperCase() : 'M' %></span>
              <% } %>
            </div>
            <div class="dropdown-menu">
              <a href="/profile">Shaxsiy kabinet</a>
              <a href="/auth/logout">Chiqish</a>
            </div>
          </div>
        </div>

        <!-- Mobil kichik ekranlar uchun yuqori chiziqda stars -->
        <span class="nav-stars nav-stars-mobile">⭐ <%= typeof user.starsBalance === 'number' ? user.starsBalance : 0 %></span>
      <% } %>
    </div>

    <!-- Mobil uchun hamburger tugma -->
    <button class="nav-toggle" type="button" aria-label="Menyuni ochish" aria-expanded="false">
      <span class="nav-toggle-line"></span>
      <span class="nav-toggle-line"></span>
      <span class="nav-toggle-line"></span>
    </button>
  </div>
</header>

<% if (typeof activeStarTest !== 'undefined' && activeStarTest) { %>
  <div class="star-banner">
    <div class="star-banner-inner">
      <div class="star-banner-text">
        <strong>⭐ Stars uchun testlar ochildi!</strong>
        <span>
          Birinchi test: <%= activeStarTest.title %>
          <% if (activeStarTest.starStartDate) { %>
            — boshlanish sanasi: <%= activeStarTest.starStartDate.toLocaleDateString('uz-UZ') %>
          <% } %>
        </span>
      </div>
      <div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;">
        <a href="/star-tests" class="btn-primary star-banner-btn">Test yechish</a>
        <a href="/star-tests/leaderboard" class="btn-outline star-banner-btn" style="background:#ffffff22;border-color:#ffffff55;color:#fff;">
          Stars test yetakchilari
        </a>
      </div>
    </div>
  </div>
<% } %>

<!-- Mobil menyu overlay va panel -->
<div class="nav-mobile-overlay"></div>
<nav class="nav-mobile" aria-hidden="true">
  <div class="nav-mobile-inner">
    <a href="/" class="nav-mobile-link">Bosh sahifa</a>
    <a href="/about" class="nav-mobile-link">Biz haqimizda</a>
    <a href="/register-course" class="nav-mobile-link">Kursga yozilish</a>
    <a href="/videos" class="nav-mobile-link">Video darsliklar</a>
    <% if (user) { %>
      <a href="/tests" class="nav-mobile-link">Testlar</a>
      <a href="/leaderboard" class="nav-mobile-link">Yetakchilar</a>
      <div class="nav-mobile-link" style="font-weight:600;display:flex;align-items:center;gap:4px;">
        <span>⭐</span>
        <span><%= typeof user.starsBalance === 'number' ? user.starsBalance : 0 %> stars</span>
      </div>
      <a href="/messages" class="nav-mobile-link nav-mobile-link-with-badge">
        <span>Adminga xabarlashish</span>
        <% if (typeof unreadFromAdmin === 'number' && unreadFromAdmin > 0) { %>
          <span class="nav-badge"><%= unreadFromAdmin %></span>
        <% } %>
      </a>
      <a href="/profile" class="nav-mobile-link">Shaxsiy kabinet</a>
      <a href="/auth/logout" class="nav-mobile-link">Chiqish</a>
    <% } else { %>
      <a href="/auth/login" class="nav-mobile-link">Kirish</a>
      <a href="/auth/register" class="nav-mobile-link">Ro‘yxatdan o‘tish</a>
    <% } %>
  </div>
</nav>

<script>
  (function () {
    var toggle = document.querySelector('.nav-toggle');
    var mobileMenu = document.querySelector('.nav-mobile');
    var mobileOverlay = document.querySelector('.nav-mobile-overlay');
    var mobileLinks = document.querySelectorAll('.nav-mobile-link');

    if (!toggle || !mobileMenu || !mobileOverlay) return;

    function openMenu() {
      toggle.classList.add('nav-toggle-open');
      mobileMenu.classList.add('nav-mobile-open');
      mobileOverlay.classList.add('nav-mobile-overlay-open');
      toggle.setAttribute('aria-expanded', 'true');
      mobileMenu.setAttribute('aria-hidden', 'false');
      document.body.style.overflow = 'hidden';
    }

    function closeMenu() {
      toggle.classList.remove('nav-toggle-open');
      mobileMenu.classList.remove('nav-mobile-open');
      mobileOverlay.classList.remove('nav-mobile-overlay-open');
      toggle.setAttribute('aria-expanded', 'false');
      mobileMenu.setAttribute('aria-hidden', 'true');
      document.body.style.overflow = '';
    }

    toggle.addEventListener('click', function () {
      var isOpen = mobileMenu.classList.contains('nav-mobile-open');
      if (isOpen) {
        closeMenu();
      } else {
        openMenu();
      }
    });

    mobileOverlay.addEventListener('click', closeMenu);

    mobileLinks.forEach(function (link) {
      link.addEventListener('click', function () {
        closeMenu();
      });
    });
  })();
</script>

<% if (typeof starWinNotifications !== 'undefined' && starWinNotifications && starWinNotifications.length) { %>
  <div id="starWinModal" style="position:fixed;inset:0;z-index:2000;display:flex;align-items:center;justify-content:center;background:rgba(0,0,0,0.45);">
    <div style="background:#ffffff;border-radius:16px;max-width:420px;width:90%;padding:20px 22px;box-shadow:0 18px 45px rgba(0,0,0,0.22);">
      <h2 style="margin:0 0 10px;font-size:20px;color:#1a3b33;">Tabriklaymiz! 🎉</h2>
      <p style="margin:0 0 8px;font-size:14px;color:#36544c;">
        Stars testlari bo'yicha yangi yutuqlaringiz mavjud:
      </p>
      <ul style="margin:8px 0 12px;padding-left:18px;font-size:14px;color:#27453d;">
        <% starWinNotifications.forEach(function(n) { %>
          <li style="margin-bottom:4px;">
            <strong><%= n.testTitle %></strong> —
            <%= n.rank %>-o'rin, <%= n.stars %> ⭐ yutdingiz.
          </li>
        <% }); %>
      </ul>
      <p style="margin:0 0 14px;font-size:13px;color:#56756a;">
        Stars balansingiz yangilandi. Omad tilaymiz!
      </p>
      <div style="display:flex;justify-content:flex-end;gap:8px;">
        <button id="closeStarWinModal" class="btn-primary" style="padding:8px 16px;font-size:14px;">Yopish</button>
      </div>
    </div>
  </div>

  <script>
    (function () {
      var modal = document.getElementById('starWinModal');
      var closeBtn = document.getElementById('closeStarWinModal');
      if (!modal || !closeBtn) return;

      function closeModal() {
        modal.style.display = 'none';
      }

      closeBtn.addEventListener('click', closeModal);
      modal.addEventListener('click', function (e) {
        if (e.target === modal) {
          closeModal();
        }
      });
    })();
  </script>
<% } %>
